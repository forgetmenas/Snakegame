# 提交记录

## 第1次提交
添加 `README.md`、`docs/甘劭涵.md`、`docs/伍竑泽.md`、`docs/王嘉宇.md` 四个文件。

## 第2次提交
新增 8 个项目文件，形成 `v1.00` 初始版本。

## 第6次提交
接入野兽与技能图片资源：将 `picture/` 中提供的 4 张野兽图和 5 张技能图迁移到 `assets/sprites/beasts/` 与 `assets/sprites/skills/` 正式目录，并按程序既有命名规则完成映射。

同时更新了图片加载逻辑，使贴图在地图与 HUD 中按比例缩放并保留合适留白，避免图标拉伸或显示过满；接入完成后删除临时 `picture/` 文件夹，并补装 `pygame` 依赖以验证项目可以正常启动。
