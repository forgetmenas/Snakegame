# 提交记录

## 第3次提交
重构项目结构，将8个源码文件按职责分入 `src/core/`、`src/entities/`、`src/systems/` 三个子包；团队成员md文件移入 `docs/`；新增 `run.py`、`.gitignore` 及各包的 `__init__.py`；所有模块import路径重写为包内绝对路径；音效加载路径更新为 `assets/` 目录；清除 `__pycache__/` 的git追踪。
